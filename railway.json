{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "dockerfilePath": "verify-api/Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}\"",
    "healthcheckPath": "/health"
  }
}
